<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.scifi.novelogic.infrastructure.mapper.CategoryMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.scifi.novelogic.infrastructure.po.CategoryPO">
        <result column="id" property="id" />
        <result column="parent_id" property="parentId" />
        <result column="category_name" property="categoryName" />
        <result column="remark" property="remark" />
        <result column="channel" property="channel" />
        <result column="order_num" property="orderNum" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, parent_id, category_name, remark, channel, order_num, create_time, update_time
    </sql>

    <select id="selectCategoryByIds" resultType="cn.scifi.novelogic.infrastructure.po.CategoryPO">
        SELECT id, parent_id, category_name, channel FROM novel_category WHERE channel=#{channel} AND id IN
        <foreach collection="categoryIds" item="categoryId" open="(" separator="," close=")">
           #{categoryId}
        </foreach>
    </select>

    <select id="selectAll" resultType="cn.scifi.novelogic.infrastructure.po.CategoryPO">
        SELECT id, parent_id, category_name, remark, channel, order_num FROM novel_category
    </select>

    <select id="selectByChannel" resultType="cn.scifi.novelogic.infrastructure.po.CategoryPO">
        SELECT id, parent_id, category_name, order_num FROM novel_category WHERE channel=#{channel}
    </select>

</mapper>
